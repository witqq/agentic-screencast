# Окружение внешних реализаций: локального синтеза Silero и шлюза
# разборчивости. Обе держатся за torch, поэтому окружение у них общее
# и лежит здесь, рядом с обеими, а не под одной из них.
#
#   cd external
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#
# Как этим пользоваться — external/README.md. Инструменту это окружение
# не нужно: он работает на узле.
torch==2.8.0
numpy
scipy
openai-whisper
