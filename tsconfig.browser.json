{
  "//": "Слой композиции собирается отдельно, потому что живёт в браузере, а не в узле. Эти файлы никто не импортирует: их читают как ТЕКСТ и вливают в страницу до документа (в подложке-снимке MHTML теги <script> не исполняются). Поэтому у них нет ни импортов, ни экспортов — и компилятор оставляет их обычными скриптами, без обёртки модуля.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM"],
    "rootDir": "src/browser",
    "outDir": "dist/browser",
    "strict": true,
    "skipLibCheck": true,
    "removeComments": false,
    "types": []
  },
  "include": ["src/browser/**/*.ts"]
}
