# Agentic Screencast — точка входа для агента

Agentic Screencast — ваш инструмент для подготовки видеопрезентации человеку.
Изучите продукт, напишите сценарий, соберите MP4 и проверьте его. CLI создаёт
слайды, получает звук и рендерит веб-кадры в заданные моменты времени.
Исследование, выбор материала и приёмка результата остаются за агентом.
Живой экран не записывается; неизменные сцены используют кэш.

## Что читать

| Вопрос | Где ответ |
|---|---|
| Установка, команды и краткий формат входа | `README.md` |
| Публичный процесс Moira, режим проверки и полномочия | `docs/moira.md`, `docs/moira.ru.md` |
| Минимальный сценарий для бесплатной сборки | `example/agent-video.md` |
| Полный формат входа, договоры о движке голоса и поставщике материала | `README.ru.md` |
| Машинно читаемое описание сцены и свой сценарий в той же форме | `agentic-screencast schema`, `agentic-screencast scenes` |
| Внешние реализации: локальный синтез, шлюз разборчивости | `external/README.md` |
| Что проверяется и как | `npm test` — сборка, типы, линтер, модульные тесты, проверки продукта |

## Три правила, без которых легко сделать неверно

1. **Вход один — файл сценария.** Слайды и данные сборки порождаются
   из него, лежат рядом с ним и правятся только через него. Руками их
   править бессмысленно: следующий запуск перезапишет.
2. **Длительность сцены выводится из речи.** Речь делится на такты
   (абзац — такт), у каждого своя запись и своя длина; длина сцены — их
   сумма. Картинка следует за звуком, а не наоборот, поэтому смена голоса
   или темпа обесценивает и звук, и кадры — так и задумано.
3. **Синтез сетевым движком стоит денег.** Ключ кэша звука — пара «текст
   такта плюс данные голоса»; изменив любую половину, вы платите за этот
   такт заново. Для проб есть движок `stub`: тишина нужной длины,
   ни сети, ни счёта. Чтобы озвучить своим голосом, есть `recorded`:
   он берёт записи из каталога и адресует их текстом реплики.

## Как запускать

Работайте в отдельном каталоге ролика. Скопируйте туда пример как `story.md`.
Для первой сборки явно выберите `stub`; `--keys-only` тоже может синтезировать
звук и не является бесплатной пробой сам по себе. Ограничения путей версии
1.0.1 и охват проверок описаны в `docs/moira.md`.

```bash
npx agentic-screencast build --source story.md --out draft.mp4 --voice-json '{"engine":"stub","name":"silent","cps":15}'
```

Для ведения всей задачи используйте публичный `admin/agentic-screencast-video`
через Moira. Сам CLI не требует Moira. Проверяйте реальные кадры готового MP4:
успешные `check` и `order` не доказывают совпадение изображения с финальной речью.

Записать озвучку голосом: `agentic-screencast record --source story.md` — поднимает
страницу на localhost, потому что микрофон браузер даёт только
в защищённом контексте.

Работает и через символическую ссылку на `dist/agentic-screencast.js` в `PATH`.
Каталог данных считается от текущего рабочего каталога либо задаётся
переменной `AGENTIC_SCREENCAST_HOME`.
